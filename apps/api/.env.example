# Copié vers apps/api/.env par `bun run setup`. Aucune valeur sensible ici :
# ce sont les identifiants par défaut de la pile Supabase locale.
ENVIRONMENT=local
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:55322/postgres
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
