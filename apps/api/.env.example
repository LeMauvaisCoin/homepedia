# Copied to apps/api/.env by `bun run setup`. Nothing sensitive here: these
# are the default credentials of the local Supabase stack.
ENVIRONMENT=local
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:55322/postgres
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
